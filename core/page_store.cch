#!ccc cch
/*
 * Document-epoch page cache (original fd + spilled add temp file).
 *
 * Live implementation is embedded at the top of piece_tree.cch —
 * Concurrent-C does not lower nested local `.cch` includes (the nested
 * file is emitted with `#\!ccc` intact). Keep the code there until that
 * is fixed; this file documents the module boundary for DESIGN/readers.
 */
#ifndef RTX_PAGE_STORE_REF_H
#define RTX_PAGE_STORE_REF_H
/* Types/API: RtxPageStore, RtxPageStore_open_original, RtxPageStore_read,
 * RtxPageStore_append_add, RtxPageStore_destroy — see piece_tree.cch. */
#endif
